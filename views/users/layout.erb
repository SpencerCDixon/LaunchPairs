<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Launch Pairs</title>
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  <link href="/css/app.css" media="all" rel="stylesheet" type="text/css" />
</head>
<body>
<div class="container">

  <section>
  <%= yield %>
  </section>

  <% if signed_in? %>
    <p>
      You are currently signed in as <%= current_user['email'] %>!
      <a href="/sign_out">Sign Out</a>
    </p>
  <% else %>
    <button class="btn btn-info"><a href="/auth/github">Sign in with GitHub</a></button>
  <% end %>

  <% if flash.any? %>
    <% if flash[:notice] %>
    <div class="alert alert-info">
      <%= flash[:notice] %>
    </div>
    <% else %>
    <div class="alert alert-danger">
      <%= flash[:negative] %>
    </div>
  <% end %>
  <% end %>

</div>
</body>
</html>
